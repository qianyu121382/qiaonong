# 巧侬网站项目协作说明

## 项目目标

本仓库用于重做 `zgqnht.com` 巧侬企业官网。巧侬与婵泉是两个完全独立的项目，不共用代码仓库、后台、数据库、环境变量、媒体目录、部署服务或备份。

## 当前阶段

- 旧站公开页面调研、第一版范围确认、总体设计、应用开发、内容迁移和正式部署均已完成。
- 技术栈为 Vue 公开前端、Vue 管理后台、Django/DRF 后端与 PostgreSQL；不采用 Wagtail。
- 正式站运行在 `https://zgqnht.com/`，`main` 检查通过后由安全 CD 自动部署精确提交。
- 当前处于生产运行、内容维护、页面精修和运维完善阶段，最新状态见 `docs/当前进度与下一步.md`。

开始工作前先阅读：

1. `docs/项目启动说明.md`
2. `docs/当前进度与下一步.md`
3. `docs/开发与验证说明.md`
4. 与任务相关的其他文档；部署任务还需阅读 `docs/CD方案与接入说明.md` 和 `docs/服务器配置与部署约束.md`

## 工作原则

- 与用户使用中文沟通。
- 用户喜欢一次讨论和确认一小块，不要一次抛出大量模型或架构设计。
- 可以只读访问旧站公开页面进行对照；未经明确授权，不登录或修改旧站后台，不提交公开表单。
- 账号、密码、服务器密钥和其他凭据不得写入仓库。
- 先记录现状，再讨论技术路线；不要因为婵泉使用 Vue + Django 就直接假定巧侬必须相同。
- 如果最终采用与婵泉相近的路线，也只能参考工作流程和通用工程经验，不复制品牌内容、图片、数据库或生产配置。
- 临时截图、浏览器自动化产物、`node_modules/`、`dist/`、本地数据库、`.env` 和媒体上传目录不得提交。
- 完成有实际影响的工作后，同步更新进度文档。

## 当前工作顺序

1. 先核对线上现状和最新成功部署提交，再定位具体问题。
2. 页面改动继续逐块确认，并验证桌面端、手机端和相关内置浏览器。
3. 内容修改优先通过巧侬管理后台完成；代码与数据职责不要混淆。
4. 代码改动必须通过现有 Checks，成功的 `main` push 会自动部署生产。
5. CI/CD、安全入口和服务器权限设计属于冻结范围，不随普通业务改动调整。
6. 持续完善独立备份、恢复演练、监控和搜索引擎收录。
