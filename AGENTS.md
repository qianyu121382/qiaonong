# 巧侬网站项目协作说明

## 项目目标

本仓库用于重做 `zgqnht.com` 巧侬企业官网。巧侬与婵泉是两个完全独立的项目，不共用代码仓库、后台、数据库、环境变量、媒体目录、部署服务或备份。

## 当前阶段

- 项目刚初始化，尚未读取或分析旧站。
- 尚未确定页面清单、数据模型和最终技术栈；第一版包含公开前端、后端和简单管理员后台，不采用 Wagtail，也不需要内容预览与发布工作流。
- 当前第一阶段是只读调研旧站公开页面，整理导航、路由、页面区块、可迁移内容及响应式表现。
- 在用户逐块确认前，不批量实现页面或后台。

开始工作前先阅读：

1. `docs/项目启动说明.md`
2. `docs/服务器配置与部署约束.md`
3. `docs/新会话启动Prompt.md`
4. 后续新增的任务相关文档

## 工作原则

- 与用户使用中文沟通。
- 用户喜欢一次讨论和确认一小块，不要一次抛出大量模型或架构设计。
- 可以只读访问旧站公开页面进行对照；未经明确授权，不登录或修改旧站后台，不提交公开表单。
- 账号、密码、服务器密钥和其他凭据不得写入仓库。
- 先记录现状，再讨论技术路线；不要因为婵泉使用 Vue + Django 就直接假定巧侬必须相同。
- 如果最终采用与婵泉相近的路线，也只能参考工作流程和通用工程经验，不复制品牌内容、图片、数据库或生产配置。
- 临时截图、浏览器自动化产物、`node_modules/`、`dist/`、本地数据库、`.env` 和媒体上传目录不得提交。
- 完成有实际影响的工作后，同步更新进度文档。

## 建议开发顺序

1. 只读调研旧站并形成页面与组件清单。
2. 与用户确认保留、删除、合并和后台维护范围。
3. 确定前端技术和第一版页面还原路线。
4. 先完成公开前端，再逐页精修桌面端与手机端。
5. 单独讨论后台数据模型和 API，仅把确实需要维护的内容做成动态数据。
6. 完成客户管理后台、安全策略、测试和数据迁移。
7. 最后处理服务器部署、域名、备案、HTTPS、备份和搜索引擎收录。
