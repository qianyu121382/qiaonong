# 巧侬开发与验证说明

## 工程基线

- Python 3.12
- Django 5.2 LTS
- Django REST Framework 3.16
- PostgreSQL 16
- Node.js 24 LTS
- Vue 3、Vue Router、Vite
- npm 与 `package-lock.json`
- uv 与锁定的 Python 依赖文件

## 目录

- `frontend/`：公开官网，开发端口 `5173`。
- `admin-frontend/`：管理后台，开发端口 `5174`，生产基础路径 `/manage/`。
- `backend/`：Django/DRF API，开发端口 `8000`。
- `deploy/`：可提交但不含凭据的部署配置模板。

## 前端命令

公开官网和管理后台分别在各自目录运行：

```bash
npm ci
npm run dev
npm run build
```

两个 Vite 开发服务器均把 `/api` 代理到 `http://127.0.0.1:8000`。

## 后端命令

先为巧侬创建独立 PostgreSQL 开发角色和数据库，再复制 `backend/.env.example` 为不提交的 `backend/.env`。

```bash
cd backend
uv venv --python 3.12
uv pip install --python .venv/bin/python -r requirements/dev.lock.txt
.venv/bin/python manage.py migrate
.venv/bin/python manage.py runserver
```

检查命令：

```bash
.venv/bin/python manage.py check
.venv/bin/python manage.py makemigrations --check --dry-run
.venv/bin/python manage.py test
```

本机 PostgreSQL 开发角色没有创建测试数据库的权限时，可使用仅供测试的内存 SQLite 配置：

```powershell
python manage.py test --settings=config.settings.test
```

该配置不会读取或修改本地开发数据库，生产环境仍固定使用独立 PostgreSQL。

健康检查：`GET /api/health/`。

### 当前 Windows 本地环境

用户已确认，当前电脑可以使用现有 `chanquan-django` Conda 环境运行巧侬后端。它只作为共享的 Python 3.13 解释器和依赖工具环境，主要依赖版本与巧侬锁文件一致。

```powershell
conda activate chanquan-django
cd E:\MyCode\Code\qiaonong\backend
python manage.py check
python manage.py runserver
```

巧侬不得读取或复用婵泉的 `.env`、数据库、数据库角色、媒体目录、管理员账号和业务数据。本地解释器可以共用，项目配置和数据仍必须隔离。

## 环境边界

- 开发默认使用 `config.settings.development`。
- 生产使用 `config.settings.production`，缺少正式密钥或允许域名时拒绝启动。
- `.env`、数据库数据、媒体目录、构建产物和日志均不提交。
- 巧侬不得使用婵泉的数据库、账号、环境变量、媒体、服务或备份。

## 自动检查

`.github/workflows/checks.yml` 使用 Node.js 24 分别构建两个 Vue 应用，并使用 Python 3.12 与 PostgreSQL 16运行 Django 检查、迁移一致性检查和测试。

## 2026-08-24 巧侬 CD 仓库待安装版本

- 新增仅支持手动触发的生产部署工作流；目标必须是属于 `origin/main` 的完整 40 位 SHA，且同一提交的 main push `Checks` 必须成功。
- 新增巧侬专用 forced-command dispatcher、root 固定部署入口和独立备份助手，只允许访问巧侬目录、仓库和 `qiaonong.service`。
- 部署入口按“同机共享锁 → 巧侬项目锁”覆盖完整部署，并要求从当前线上提交 fast-forward。
- 部署前强制备份巧侬数据库和两个旧 `dist`；构建后只放宽公开 `dist` 的读取与遍历权限。
- 生产检查包含 Gunicorn Socket、`nginx -t` 及正式证书配合 `--resolve` 的本机 HTTPS，不使用 `--insecure`。
- 迁移开始前失败自动恢复；迁移开始后保留快照并转人工处理，不自动反向迁移或盲目恢复旧代码。
- 已只读核对巧侬生产目录、服务、Socket、数据库与角色名称、构建权限和 HTTPS；没有修改服务器 SSH、sudoers、`/usr/local/sbin`、虚拟环境、服务或任何婵泉资源。
- 详细信任边界、Environment Secrets 和后续安装演练步骤见 `docs/CD方案与接入说明.md`。
- 最终共同规范明确采用 `qiaonong-cd` 专用传输用户、root-owned 固定入口和 `qiaonong:www-data` 三层职责；部署备份由 root 保护，项目用户只流式生成数据库转储。
- 本轮只形成并验证仓库待安装版本，不修改服务器。提交通过 main push Checks 后，CI/CD 文件、部署目录、CD 方案文档和服务器权限设计进入冻结状态。

## 2026-08-25 巧侬生产 CD 首次安装与验收

- 已为巧侬安装独立的 `qiaonong-cd` 传输账号、forced-command Key、最小 sudoers、root-owned 固定脚本和强化后的 `qiaonong.service`。
- `qiaonong` 运行用户保持不可登录且无 sudo；Git、pip、npm、Django 与 Gunicorn 均由 `qiaonong:www-data` 执行。
- `/srv/qiaonong/venv` 已调整为 `qiaonong:www-data`，部署备份目录已调整为 `root:root 0700`。
- 共享锁与巧侬项目锁均由 root 创建为 `0640`，部署入口按“共享锁 → 项目锁”顺序获取。
- 本地与服务器端脚本语法检查及 SHA-256 核对通过；固定脚本、service 和 sudoers 已由 root 原子安装。
- Shell、非法 SHA、短 SHA、SCP、SFTP、端口转发及跨项目命令均已验证拒绝；合法当前 SHA 的幂等健康检查通过。
- GitHub Environment `qiaonong-production` 和四项 `QIAONONG_` SSH Secrets 已配置，GitHub 只向服务器传递目标 SHA。
- 首次真实 CD 成功部署 `601589d475d3c4e9e6c273133c17c6f81eb31083`；工作流 run 为 `32751668015`。
- 部署自动完成数据库与双前端快照、生产依赖安装、双前端构建、迁移检查、生产检查、迁移、静态收集、Nginx 检查及 `qiaonong.service` 重启。
- 生产 Git SHA、Gunicorn Socket、官网 HTTPS、管理后台 HTTPS、公开接口和构建目录权限均已验收通过。
- CI/CD 文件、`deploy/`、CD 方案文档和服务器权限设计从本次验收起保持冻结，除非用户明确要求或发现阻断部署的真实缺陷。

## 2026-08-25 巧侬自动 CD

- 经用户明确要求临时解冻 `.github/workflows/deploy.yml`，保留原有手动指定 SHA 的发布入口，并增加 `Checks` 完成后的自动入口。
- 自动入口仅接受当前仓库 `main` 分支的成功 push Checks，目标版本直接取该次 Checks 的完整 `head_sha`；pull request、失败、取消以及非当前仓库来源均不触发生产部署。
- 自动和手动入口共用原有 `qiaonong-production` Environment、独立并发组、精确 SHA CI 校验、受限 SSH、服务器共享锁与项目锁，不修改服务器固定入口。
- 本轮通过 CI 并完成首次自动部署验收后，CI/CD 文件和方案文档重新进入冻结状态。

## 2026-08-09 基线验证

- 公开前端生产构建通过。
- 管理前端生产构建通过。
- 两个前端依赖审计均未发现漏洞。
- Django 系统检查通过。
- Django 迁移一致性检查显示没有待生成迁移。
- 健康检查自动化测试通过。
- 本机创建 Python 3.12 Conda 环境时受到 Conda 官方源 TLS 错误阻挡；经用户确认，本地 Django 检查使用已存在且依赖版本一致的 `chanquan-django` Python 3.13 环境执行。Python 3.12 与 PostgreSQL 16 的生产目标组合由 CI 配置覆盖，需在首次推送后确认实际运行结果。

## 2026-08-10 本地数据库与联通验证

- 已创建巧侬独立的本地 PostgreSQL 角色和数据库 `qiaonong_dev`。
- 已创建由 Git 忽略的 `backend/.env`，数据库密码未写入仓库或文档。
- Django 内置初始迁移全部成功应用到巧侬数据库。
- Django 系统检查、迁移一致性检查和健康检查测试再次通过。
- `http://127.0.0.1:8000/api/health/` 返回 `200`。
- 公开前端代理 `http://127.0.0.1:5173/api/health/` 返回 `200`。
- 管理前端代理 `http://127.0.0.1:5174/api/health/` 返回 `200`。
- 联通检查结束后，临时启动的三个开发服务均已停止。

## 2026-08-10 首次 CI 验证

- 工程基线提交 `3fdf312` 对应的 GitHub Actions 全部通过。
- 公开前端和管理前端均在 Node.js 24 环境完成依赖安装及生产构建。
- 后端在 Python 3.12 与 PostgreSQL 16 环境完成系统检查、迁移一致性检查和测试。
- GitHub Actions 官方组件已更新到 Node.js 24 运行时版本，消除旧版 Node.js 20 运行时弃用警告。
- 4.2 工程准备完成，下一步进入第 5 阶段并逐个确认、开发业务模块。

## 2026-08-10 产品模块后端验证

- 已生成并应用产品模块初始迁移 `catalog.0001_initial`。
- Django 系统检查及迁移一致性检查通过。
- 产品模块自动化测试覆盖分类层级、公开可见性、筛选搜索、管理权限及保护删除。
- 本地 `qiaonong_dev` 角色不额外授予创建数据库权限，完整测试使用 CI 中隔离的 PostgreSQL 测试服务执行。

## 2026-08-10 第一版端到端实现

- 管理员 Session/CSRF 登录、会话恢复、退出和登录限流已实现。
- 网站设置、内容页面、首页轮播、分类、产品和产品图片管理 API 已实现。
- 管理前端已实现登录及上述内容的基础维护界面。
- 公开前端已实现首页、品牌、分类、产品详情、搜索、联系和政策页面及响应式布局。
- 本地通过管理前端代理实际验证登录、读取 22 个分类和退出，临时验证账号随后已删除。
- 旧站归档成功导入 22 个分类、50 个下架产品及 68 个本地媒体文件；媒体文件未加入 Git。
- 旧站内容页只创建待核验空壳，没有迁移旧站中的婵泉公司、地址、备案或账号信息。
- 已提供生产环境变量、Nginx、systemd、部署检查和独立备份模板；尚未连接或修改服务器。

## 2026-08-10 第一版远端验证

- 第一版提交 `719d94e` 对应的 GitHub Actions 全部通过。
- Python 3.12 与 PostgreSQL 16 环境运行 23 项 Django 测试，结果全部通过。
- Django 开发设置检查、生产设置 `check --deploy` 和迁移一致性检查全部通过。
- 公开前端与管理前端均完成依赖审计，未发现漏洞，并成功生成生产构建。
- 代码、迁移入口和部署模板已推送；生产服务器仍未连接或修改，需用户验收真实资料和页面后再部署。

## 2026-08-10 本地完整内容预览

- 经用户要求，本地数据库中的 50 个迁移产品已全部临时上架，用于查看公开前端完整效果；该状态不会自动同步到生产数据库。
- 50 个产品均具备封面图，另保留 1 张确实存在的额外产品图片；分类及产品媒体共 68 个文件，约 12.84 MiB。
- 已修正重复执行迁移命令时可能把封面重复写入产品相册的问题，并清理本轮生成的 50 个重复文件。
- 品牌、联系和政策页仍保持待核验状态，没有导入旧站中的婵泉主体、地址或备案信息。

## 2026-08-10 旧站实时资料补充与页面对比

- 再次只读访问旧站公开 HTTP 页面，首页、品牌、产品和联系页当前可打开；HTTPS 证书仍不能正常校验。
- 可确认属于巧侬的公开资料已经加入本地预览：`巧侬花田`站名、Logo、6 张首页横幅、5 张一级系列图、品牌短文和客服电话 `13596956311`。
- 首页旧站“明星爆款”对应的 4 个产品已设为推荐；本地仍为 50 个已上架产品，便于用户一次查看完整效果。
- 新版保留旧站品牌、分类和产品内容，但采用单行紧凑导航、清晰的产品卡片与详情结构，并提供真正的手机端重排；旧站手机端固定约 1400px 宽、横向裁切的布局没有照搬。
- 旧站未发现可核验的巧侬邮箱或地址。页脚中的辽宁婵泉公司名、鞍山地址和备案号没有迁移；空白的使用政策、隐私条款与 Cookies 政策也没有虚构正文。
- 本地媒体现为 81 个文件、约 47.08 MiB；这些媒体由 Git 忽略，部署时须通过巧侬独立媒体迁移流程传送。

## 2026-08-10 公开站导航下拉优化

- 桌面端产品导航改为带栏目标题、双列子栏目和“查看全部”入口的分层面板；子栏目较少时自动使用紧凑单列宽度。
- 只有实际存在二级栏目的一级分类才显示下拉箭头，悬停与键盘聚焦均可打开菜单。
- 手机端继续使用纵向展开导航，不显示桌面端面板装饰；在 `390×844` 视口验证菜单可展开且页面无横向溢出。
- 公开前端生产构建通过，并在本地真实分类数据下完成桌面悬停和手机菜单检查。
- 顶部导航已补充明确的“首页”入口，桌面端与手机端均可直接返回 `/`，Logo 入口继续保留。

## 2026-08-10 首页轮播交互补充（本地待确认）

- 首页有多张横幅时每 5 秒自动切换，鼠标停留或键盘聚焦轮播区域时暂停。
- 配置了链接的横幅支持整张点击跳转，轮播圆点保持独立手动切换，不会误触横幅链接。
- 已用真实 6 张横幅验证第 1 张可自动切换至第 2 张，当前横幅点击后可进入其配置的站内目标路径。
- 本轮按用户要求仅保留本地修改，尚未提交或推送。

## 2026-08-10 页脚精简与微信入口（本地待确认）

- 页脚移除当前没有有效正文的使用政策、隐私条款和 Cookies 政策入口，保留品牌、首页、品牌介绍、产品中心、联系我们及已核验联系方式。
- 从旧站公开归档补入带巧侬标识的微信二维码；桌面端悬停或聚焦微信按钮显示二维码，手机端直接显示二维码。
- 政策页面和后台数据暂未删除，仅从公开页脚隐藏，避免未经确认执行不可逆的数据删改。
- 本轮继续按用户要求只保留本地修改，尚未提交或推送。
- 页脚随后进一步压缩为桌面端单行信息条与轻量版权栏，手机端紧凑换行；二维码改回通过微信按钮悬停或聚焦展开，不再直接占据页脚高度。

## 2026-08-10 首页产品系列重构（本地待确认）

- 首页不再平铺 6 张固定系列卡片，改为系列标签驱动的单个展示区；切换标签会同步更新主图、二级栏目、代表产品和系列入口。
- 每个系列动态读取分类 `banner`、`description`、子栏目以及该分类前 3 个公开产品，不新增硬编码产品内容，也不增加后台模型。
- 桌面端采用大图与内容分栏，手机端采用横向可滑动系列标签和纵向内容布局。
- 已用真实数据验证“护肤”与“眼部系列”切换、子栏目及代表产品更新；`390px` 视口无页面横向溢出，生产构建通过。
- 本轮仍按用户要求只保留本地修改，未提交或推送。

## 2026-08-10 首页视觉素材替换（本地待确认）

- 原首页品牌图本身带“关于我们”、Logo 和整段文案，与新版左侧品牌文字重复；已替换为无文字、无 Logo 的新品牌氛围图。
- 为护肤、眼部、水光、彩妆、院护和医美分别生成 6 张横向系列图，构图按当前大图容器保留安全裁切区域，不再放大旧竖卡图片。
- 生成图统一采用米白、深绿、炭黑和琥珀色视觉体系；不包含婵泉内容、人物、文字、虚构品牌标识或可读包装标签。
- 7 张 PNG 源图经 WebP 优化后合计约 675 KiB，保存于巧侬本地 `backend/media/content/generated/2026/08/`；原媒体文件保留，可随时从后台切回。
- 已验证桌面与 `390px` 手机视口的品牌图、系列图裁切；手机系列标签保持横向滑动并隐藏原生滚动条。
- 图片与数据库调整仍只存在于本地，未提交、未推送，也未同步到生产环境。

## 2026-08-10 导航内页横幅替换（本地待确认）

- 品牌介绍页使用新的无文字品牌氛围图作为顶部横幅，不再在正文中重复展示整张旧站排版图。
- 护肤、眼部、水光、彩妆、院护和医美栏目页使用各自生成的横向系列图；二级栏目继承对应一级系列图，保持视觉一致。
- 删除产品栏目页“浏览巧侬已经公开的产品资料”通用占位句；只有后台确实填写分类简介时才显示说明。
- 搜索结果和全部产品页没有对应系列图片时继续使用简洁默认横幅，不伪造栏目素材。
- 本轮继续只保留本地修改，未提交或推送。
- 联系我们页已移除“以下联系资料由巧侬管理员维护。”前端占位说明；只有后台填写正式摘要时才会显示副标题。

## 2026-08-23 ICP 备案号更新

- 用户确认巧侬新备案号为 `辽ICP备2026018730号-1`，网站备案链接使用工信部备案系统 `https://beian.miit.gov.cn/`。
- 公安联网备案尚未完成，本次不添加公安备案号或相关链接。
- 旧站曾显示的婵泉备案信息继续保持排除，不得迁入巧侬。

## 2026-08-23 当前运营公司补充

- 用户确认当前运营公司为 `鞍山鼎禾生物制药有限公司`，网站品牌名继续使用 `巧侬花田`。
- 公司名称用于页脚版权主体、联系我们页公司名称以及搜索引擎企业结构化数据；结构化数据同时保留 `巧侬花田` 作为品牌别名。
- 本次不填写尚未确认的公司地址、统一社会信用代码等信息，也不将已注销的旧个体工商户描述为公司更名或法律继承关系。

## 2026-08-23 首页轮播响应式图片

- 仅参考婵泉的响应式实现思路，在巧侬独立模型中为轮播图增加可选的手机端图片，不复制婵泉代码、媒体或数据。
- 前台使用 `<picture>` 在 `640px` 及以下加载手机图；未上传手机图时自动使用原桌面图兜底。
- 桌面轮播容器保持当前素材的 `1575 / 590` 比例，手机端图片与容器统一切换为 `8 / 5`，图片继续使用 `object-fit: cover`。
- 巧侬管理后台支持分别上传桌面端和手机端图片，并明确显示手机图是否使用桌面图兜底。
- 现有 6 张轮播图的手机字段保持为空，待单独制作并确认巧侬手机版素材后再上传；本次不迁入婵泉图片。
- 后端 28 个测试、Django 检查、迁移一致性检查、公开前端与管理后台生产构建均已通过。

## 2026-08-23 客户管理后台范围收缩

- 客户管理后台只保留产品管理、首页轮播和公司信息三个入口。
- 产品可以新增、编辑、删除、上下架、推荐并维护图片；所属分类只能从既有分类中选择，产品 URL 标识和旧站 ID 由系统管理。
- 首页轮播继续支持电脑图、手机图、链接、排序和启用状态的完整维护。
- 公司信息只允许维护公司名称、联系方式、Logo、微信二维码、备案资料和页脚补充文字；首页文案、固定页面及网站结构不再向客户开放。
- 产品分类和内容页面的管理 API 保留读取能力，但创建、修改和删除均返回 405，不能依靠隐藏前端按钮绕过限制。
- 管理前端遇到非 JSON 的 Django 错误页时改为显示简短状态提示，不再把整页 HTML 源码展示给用户。
- 本地数据库已确认迁移完整，业务内容字段中未发现百度验证代码或 HTML 页面源码；后端 28 项测试、Django 检查、迁移一致性检查及管理前端生产构建均通过。

## 2026-08-24 产品分类管理重新开放

- 用户确认产品分类应继续与官网顶部导航、下拉菜单、首页系列及产品列表自动联动。
- 客户管理后台新增“产品分类”入口，支持一级和二级分类的新增、查询、编辑、排序、启停和简介维护，并支持一级分类栏目图维护。
- 栏目图只向一级分类开放；二级分类页面继承所属一级分类横幅。旧站迁入的二级分类图片暂时保留在独立数据库中，不批量删除，也不在客户后台继续维护。
- 分类最多保持两级；分类 URL 标识和旧站 ID 由服务器管理，客户修改名称时不会破坏已有公开链接。
- 存在下级分类或关联产品时拒绝删除，并返回可读提示；此时应调整关联关系或停用分类，避免误删产品结构。
- 停用一级分类后，现有公开 API 会同步隐藏其下级分类和相关产品；重新启用后恢复公开。
- Django 检查、迁移一致性检查、30 项后端测试及管理前端生产构建均已通过；本轮没有修改数据库结构。

## 2026-08-25 产品分类页栏目图纯图片展示

- 产品分类页顶部栏目图区域不再渲染英文标题、分类名称或分类简介，是否配置栏目图均不会恢复这些文字。
- 已配置的栏目图取消深色渐变遮罩，按原图直接展示；分类名称继续保留在顶部导航和分类侧栏中。

## 2026-08-25 首页系列图全幅背景展示

- 桌面端将 `1920×378` 栏目图完整置于贯穿展示区的清晰背景层，另以同图的柔化暗色版本铺满上下余量，避免长图被强行放大裁切或出现生硬留白。
- 右侧系列资料使用 58% 不透明度的米白玻璃面板和背景模糊，仍能看见延伸到内容区的栏目图，同时保证文字与产品入口可读。
- 手机端按素材原始宽高比完整展示栏目图，系列资料在图下恢复高可读性的浅色纵向布局，避免长图在狭窄屏幕中被裁切或被纵向拉伸。

## 2026-08-25 首页产品系列展台精修（全幅画幅与一体化精选展台）

- 针对 1920×378 超长横幅（5.08:1）在双栏并排时右侧产品被半透明面板遮挡、左右分割生硬的问题进行独立重构与美化。
- 顶部设立独立全幅画幅视口（`1920 / 378` 比例完整呈现原图左侧系列标题、居中品牌标与右侧产品合影），悬停呈现微缩放与晶透探索胶囊标签，整幅支持点击跳转。
- 下部采用一体化精选展台（`#ffffff` 卡片底色搭配柔和阴影与细边框）：左侧为系列标题、简介、子系列分类标签与全系列直达按钮；右侧并排展示 3 款代表单品卡片（纯白内衬、单品图自适应包含、悬浮微抬升与详情动效）。
- 平板与手机端自然收叠为垂直层叠布局，手机端标签保持横向流畅滑动并隐藏滚动条，单品卡片紧凑三列并排，全端无横向溢出且构建测试通过。

## 2026-08-28 公安联网备案标识

- 用户确认巧侬公安联网备案已通过，备案号为 `辽公网安备21030302000219号`。
- 公安备案号与查询链接作为低频变更的静态合规信息加入公开站页脚，不新增后端字段、数据库迁移或管理后台入口。
- 页脚使用用户提供的公安备案图标，点击备案信息在新窗口打开公安部查询页 `https://beian.mps.gov.cn/#/query/webSearch?code=21030302000219`。

## 2026-08-31 产品分类管理一致性修正（本地待提交）

- 管理后台分类树的启停筛选同时作用于一级和二级分类，不再在仅设置状态筛选时显示全部子分类。
- 产品表单的分类选项按一级分类分组排列，并展示二级分类所属关系与实际公开状态；新增产品不会默认落入已停用或因上级停用而隐藏的分类。
- 产品列表按一级分类筛选时覆盖该一级分类自身及其全部二级分类，行为与公开产品接口一致。
- 二级分类启用但一级分类停用时，分类页以“上级停用”明确展示继承后的实际公开状态。
- `AdminCategorySerializer` 强制二级分类的 `banner` 为空；分类从一级调整为二级时会清除旧栏目图引用。产品分类 SEO HTML 对二级分类改为读取所属一级分类栏目图。
- 增加后端回归测试，覆盖一级分类调整为二级分类后清除栏目图，以及二级分类 SEO 分享图继承一级栏目图。
- 未新增数据库字段或迁移，未批量删除历史媒体文件，未修改 CI/CD 和生产服务器配置。
- 已通过 32 项完整后端测试、Django 系统检查、`makemigrations --check --dry-run --settings=config.settings.test` 及管理后台 `npm run build`。
