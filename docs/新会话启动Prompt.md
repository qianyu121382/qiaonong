# 新会话启动 Prompt

请复制下面内容给新的 Codex 会话：

---

我要开始一个全新的“巧侬企业官网”重做项目，工作目录是：

`E:\MyCode\Code\qiaonong`

旧站域名是：

`zgqnht.com`

请先完整阅读仓库中的 `AGENTS.md`、`README.md` 和 `docs/项目启动说明.md`，并遵守其中的项目边界。

这是一个独立项目。另一个婵泉项目虽然已经做过，但巧侬不得与它共用代码仓库、后台、数据库、环境变量、媒体目录、部署服务或备份，也不要复制婵泉的品牌内容、图片、账号和生产数据。可以参考婵泉项目采用过的开发流程：先调研旧站、整理页面清单、逐块确认、先做公开前端、再讨论动态模块与后台、最后部署和 SEO，但不要预设巧侬的网站结构与技术方案一定相同。

当前先不要写网站代码。第一步请只读访问 `zgqnht.com` 的公开页面，不提交任何表单，也不要尝试修改旧站。请整理：

1. 一级导航和二级栏目；
2. 哪些栏目有独立 URL，哪些只是页面内区块或锚点；
3. 每个页面由哪些主要组件组成；
4. 产品、新闻、联系表单等可能需要后台维护的模块；
5. 桌面端与手机端的主要差异；
6. 旧站图片、文字和详情内容的大致迁移规模；
7. 明显异常、失效内容以及不建议照搬的部分。

把调研结果保存在 `docs/` 下，文件名使用中文。先给我一个简短结果并和我逐块确认，不要一次输出大量架构或数据模型，也不要在未经确认时开始完整实现。

我熟悉 Python、Django、Java、Scala、Vue 和 React。技术栈等页面范围明确后再一起决定，不默认使用 Wagtail。

---

