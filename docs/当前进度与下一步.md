# 巧侬当前进度与下一步

最后核对：2026-08-30

## 当前结论

巧侬第一版已经完成开发、数据迁移、正式域名切换、HTTPS、备案信息接入和安全 CD 部署，目前处于生产运行、内容维护与持续精修阶段，不再处于旧站调研、需求设计或部署前阶段。

- 正式官网：`https://zgqnht.com/`
- 管理后台：`https://zgqnht.com/manage/`
- 健康检查：`https://zgqnht.com/api/health/`
- 当前仓库与最近一次生产自动部署提交：`40e29f1 feat: 添加公安联网备案标识`
- 2026-08-30 只读复核时，官网、管理登录入口和健康检查均正常，健康接口返回 `status: ok`。
- 提交 `40e29f1` 的 GitHub Checks 与自动 `Deploy production` 均成功。

生产提交的最终权威来源仍是服务器 `/srv/qiaonong/app` 的 `HEAD` 与最近一次成功的 GitHub Deploy production 记录；本文只记录最近一次已核对结果。

## 已完成范围

### 公开网站

- Vue 3 + Vite 公开前端。
- 首页、品牌介绍、产品中心、一级/二级产品分类、产品详情、站内搜索、联系我们和真实 404。
- 桌面端与手机端响应式导航、轮播、产品系列、列表、详情和页脚。
- 首页轮播支持独立手机图。
- 分类页栏目图采用纯图片展示；首页产品系列采用全幅长图与精选产品展台。
- 旧 PHP 栏目和产品 URL 支持永久重定向。

### 后端与管理后台

- Django 5.2 + Django REST Framework + PostgreSQL。
- 独立 Vue 管理后台，使用 Django Session 与 CSRF。
- 产品分类、产品、产品图片、首页轮播和公司信息维护。
- 产品分类最多两级，并与官网导航、首页系列和产品列表联动。
- 产品上下架、推荐、排序、图片校验、分类可见性和保护删除规则。
- 管理登录限流及公开只读 API、管理写 API 的权限隔离。

### SEO 与合规

- HTTPS 根域名 `zgqnht.com` 为规范地址；HTTP 与 `www` 统一 301。
- 动态 `robots.txt`、`sitemap.xml`、canonical、页面标题、描述、Open Graph 与结构化数据。
- Django 为公开路由输出含真实内容的可抓取 HTML，Vue 加载后接管交互。
- ICP 备案：`辽ICP备2026018730号-1`。
- 公安联网备案：`辽公网安备21030302000219号`。
- 当前运营公司：`鞍山鼎禾生物制药有限公司`；品牌名为“巧侬花田”。

### 部署与运维

- Nginx、Gunicorn、systemd、PostgreSQL、Certbot 与独立媒体目录已经投入生产。
- 巧侬与婵泉保持独立仓库、应用目录、虚拟环境、数据库/角色、环境变量、媒体、服务、Socket、Nginx 站点和备份。
- GitHub Checks 覆盖双前端构建与依赖审计、30 项 Django 测试、生产设置检查、迁移一致性和 CD 模板检查。
- `main` 的 push Checks 成功后，会自动部署对应的精确提交；失败、取消、PR 或非本仓库来源不会进入生产。
- 安全 CD 使用巧侬专用 Environment、Secrets、SSH forced command、固定 root 入口、共享锁/项目锁、部署前快照和分阶段回滚规则。

## 线上内容基线

2026-08-30 通过公开 API 只读核对：

- 一级产品分类：6 个。
- 一级与二级分类合计：26 个。
- 公开产品：88 个。
- 首页轮播：6 张。
- 公开内容页：品牌介绍、联系我们，共 2 个。
- 站名、公司名称、电话和 ICP 信息均从巧侬独立后端正常返回。

这些数字会随管理员日常维护变化，不能长期当作固定代码常量。

## 2026-08-30 公共前端手机端改造（本地待提交）

- 以巧侬现有 Vue 公共前端为基础，参考婵泉项目的手机端响应式原则完成适配；未复制其品牌、文案、图片、业务数据或接口。
- 响应式样式统一使用 `max-width: 900px` 与 `max-width: 520px` 两级断点，页面最小宽度仍为 320px；Vite 的 `build.target` 与 `build.cssTarget` 已固定为 `chrome97`。
- 手机端 Header 调整为 64px 顶部粘性结构，Logo 完整显示，汉堡菜单位于右侧；菜单在 Header 下方全宽展开，导航跳转后自动关闭。
- 新增 48px 固定底部联系栏，仅提供当前项目真实存在的电话、产品中心与联系我们入口；页脚包含安全区域与遮挡补偿。
- 内页 Banner 改为真实图片元素，900px 以下按素材原比例显示；手机端标题、品牌正文、产品分类横向导航和内容间距已压缩。
- 首页保留独立手机轮播图，增加单图停止自动轮播、失败图片跳过及本地备用图；首页品牌介绍在手机端继续隐藏图片。
- 产品列表在手机端保持两列和方形图片，产品名预留两行；详情页在 520px 以下改为主图在上、文字自然展开；联系我们页保持现有信息展示，不新增留言表单或虚构客服入口。
- 已执行 `npm run build` 并通过。产物 CSS 只包含传统 `@media (max-width: 900px)`、`@media (max-width: 520px)` 与减少动态效果查询。
- 已用本地开发站与只读生产 API 数据检查 390px、375px、320px，以及 1024px、1280px、1440px；首页、品牌页、产品分类、产品详情和联系我们均无页面级横向滚动，手机底栏未遮挡页脚内容，桌面布局未发现明显回归。
- 本轮未修改客户管理后台、后端接口、数据模型、生产部署或服务器配置；上述结果仍是本地状态，提交并通过既有部署流程后才会进入生产。

## 当前工作重点

1. 提交并部署本轮公共前端手机端改造，再进行真实设备及微信/百度等常用内置浏览器的视觉与交互验收。
2. 由管理员持续核验产品分类、产品图片、成分、功效及医美相关表述。
3. 观察搜索引擎抓取、服务器磁盘、内存、Swap、日志、媒体和备份增长。
4. 补齐媒体定期备份、备份保留周期、自动清理和异机备份，并安排独立恢复演练。
5. 后续普通业务改动不得顺带修改已冻结的 CI/CD、安全入口和服务器权限设计；只有用户明确要求或发现真实阻断缺陷时才解冻。

## 文档状态说明

- `旧站公开页面调研.md`、`项目定义.md`、`第一版范围.md`、`可行性与风险.md` 和 `总体设计与工程准备.md` 记录项目形成过程，保留历史价值。
- `第一版实现与部署前检查.md`、`部署会话交接说明.md` 和首次部署前段落属于历史部署资料，不代表当前仍未上线。
- 当前状态优先以本文、`开发与验证说明.md`、`首次预览部署记录.md` 和 `CD方案与接入说明.md` 为准。
