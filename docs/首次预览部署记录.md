# 首次预览部署记录

## 2026-08-11

- 预览服务器部署提交：`18d47a41eb625c24af3c8d2d6cff68ee1df7edc5`。
- 已创建巧侬独立的 Linux 用户、应用目录、Python 虚拟环境、PostgreSQL 角色与数据库、Gunicorn socket、systemd 服务及 Nginx 站点。
- 已完成数据库迁移和旧站数据初始化：22 个启用分类、50 个启用产品、4 个推荐产品、6 张启用轮播图、5 个内容页和 1 份网站设置。
- 已独立传输并校验 89 个媒体文件，共 50,091,707 字节。
- 前台、产品页、管理入口、健康检查、公开分类、公开产品、网站设置、媒体响应、管理员会话登录及旧链接重定向均已通过预览检查。
- 当前仅提供 HTTP Hosts 映射预览；尚未修改正式 DNS、关闭旧站、申请 HTTPS 或启用 HSTS。
- 生产凭据、服务器地址和环境变量不记录在仓库中。

## 后续事项

1. 人工验收桌面端、手机端、管理后台编辑及图片上传。
2. 确认正式主域使用根域名还是 `www`，并确定统一 301 方向。
3. 验收通过后切换 DNS，再申请 HTTPS 证书并启用安全 Cookie、HTTPS 跳转和 HSTS。
4. 配置巧侬独立备份并至少完成一次恢复演练。

## 2026-08-23 ICP 备案更新

- 巧侬新 ICP 备案号 `辽ICP备2026018730号-1` 已加入生产站点页脚，并链接工信部备案系统。
- 本次生产部署提交为 `823ae58`；部署前已完成巧侬独立数据库与媒体备份。
- 生产主页、管理入口、健康检查、分类接口和网站设置接口均返回 200，备案号及链接已通过生产 API 验证，`qiaonong.service` 运行正常且无新增错误日志。
- 服务器当时无法解析公网域名，导致基于公网域名的检查脚本未能运行；随后通过服务器本机 Nginx 路径和外部直连完成了等价验证。
- 公安联网备案后续单独办理，本次部署不展示尚未取得的公安备案信息。

## 2026-08-23 当前运营公司更新

- 网站当前运营公司补充为 `鞍山鼎禾生物制药有限公司`，品牌名继续使用 `巧侬花田`。
- 生产部署提交为 `e56c5e8`；部署前已完成巧侬独立数据库与媒体备份。
- 公开网站设置接口已返回新公司名称；首页企业结构化数据同时包含公司法定名称和 `巧侬花田` 品牌别名。
- `qiaonong.service` 重启后运行正常，生产接口、首页结构化数据及错误日志检查均通过。

## 2026-08-11 正式域名切换

- 已完成手机端关键页面检查：首页、产品列表、产品分类、产品详情、品牌介绍和联系我们均无页面级横向溢出、破图或接口错误。
- 根域名和 `www` 已切换至新服务器，公共 DNS 解析验证通过。
- 已为 `zgqnht.com` 和 `www.zgqnht.com` 签发 Let's Encrypt 证书，并启用自动续期。
- 主站统一为 `https://zgqnht.com`；HTTP 及 `www` 请求统一使用 301 跳转到 HTTPS 根域名。
- 已启用安全 Cookie、HTTPS 强制跳转和 300 秒短期 HSTS；稳定运行后再评估延长 HSTS。
- 公共首页、管理入口、健康检查、分类、网站设置、产品详情及静态资源均已通过 HTTPS 检查。

## 2026-08-11 百度抓取优化

- 新增真实的 `robots.txt` 与动态 `sitemap.xml`，站点地图自动包含公开分类、产品和内容页。
- 为公开页面增加 canonical、独立标题、描述、Open Graph 信息以及组织/产品结构化数据。
- 公开路由由 Django 输出包含真实正文的可抓取 HTML，Vue 加载后继续接管原有交互；后台内容更新后无需重新构建 SEO 页面。
- 首页、品牌、联系、产品列表、分类、产品详情和政策页面均支持服务端预渲染；不存在的公开地址返回真实 404，站内搜索标记为 `noindex`。
- Nginx 将上述公开页面及抓取资源转发给巧侬 Gunicorn，静态资源、媒体与管理后台仍由 Nginx 独立提供。
